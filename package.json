{
  "name": "jsx-hackernews",
  "version": "0.5.0",
  "description": "Sample Hacker News app using jsxrender.",
  "main": "dist/bundle.js",
  "scripts": {
    "build": "rm -rf dist && tsc -b . demo demo/sw scripts && (cd demo; node ../dist/bundle.js)",
    "watch": "tsc -b scripts demo/sw demo -w",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Martyn Tebby",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^16.4.7",
    "@types/react-dom": "^16.0.6",
    "csstype": "^2.5.6",
    "typescript": "^3.0.1"
  },
  "dependencies": {
  }
}
